# amc-newsroom-demo2026

2026 서울아산병원 뉴스룸 웹·앱 통합개편 **시연용 정적 프로토타입**입니다.
직원 의견 취합 결과와 회의 결정 사항을 실제 화면으로 옮겨, 관련 부서·업체 시연에 그대로 사용합니다.

- 시연 주소: https://daye8965.github.io/amc-newsroom-demo2026/start.html
- 제작: 홍보팀 언론Unit · 내부 검토용

## 성격

실제 서비스가 아닙니다. **기사·인물·수치는 모두 가상 예시**이며 실제 직원·의료진 이름은 쓰지 않습니다.
데이터·검색·업로드는 동작하지 않습니다(프레스센터의 복사·다운로드는 예외로 실제 동작).
새 화면이나 예시를 만들 때도 이 원칙을 지키세요.

빌드 도구·프레임워크 없는 순수 정적 HTML입니다. 파일을 그대로 열어도 동작해야 합니다
(시연 장소 인터넷이 불안하면 ZIP을 받아 `start.html`을 더블클릭하는 방식을 안내하고 있습니다).

## 화면

| 파일 | 화면 |
|---|---|
| `start.html` | 시연 시작점. 7단계 순서와 사용법 안내 |
| `compare.html` | 현재 vs 개편안 비교 |
| `admin.html` | ★ 업로드 페이지(CMS). 현재 화면 ↔ 개편 가안 전환, 「문제 지점 표시」 |
| `index.html` | 뉴스룸 메인 |
| `article.html` | 기사 상세 |
| `press.html` | ★ 프레스센터. 본문 복사·사진 다운로드가 실제 동작 |
| `app.html` | 앱 화면 |
| `guide.html` | 개편 포인트 정리, 요구사항 18건 매핑표, 로드맵 |

공용 자원: `assets/css/main.css`, `assets/js/site.js`, `assets/js/data.js`

## 화면 속 3가지 도구

- **개편 포인트** — 모든 화면 우측 하단. 번호가 붙은 설명이 뜨고, 무엇이 바뀌었고 근거가 어느 회의의 어떤 결정인지 함께 보입니다.
- **현재 화면 / 개편 가안** — `admin.html` 상단. 같은 화면의 두 버전을 오갑니다.
- **문제 지점 표시** — `admin.html`의 '현재 화면'에서만. 어느 필드가 왜 불편한지 주황색으로 표시합니다.

## 작업 규칙

- **근거를 함께 적습니다.** 화면을 바꾸면 해당 「개편 포인트」 주석도 같이 고치고, 근거(회의 날짜·결정 사항, 분석 리포트 문구)를 남기세요. 이 저장소의 설득력은 여기서 나옵니다.
- 커밋 메시지는 한글 서술형 한 줄입니다. 예) `기사 상세의 공유를 제목 옆 버튼으로 이동`
- 외부 라이브러리를 새로 끌어오지 마세요. 오프라인에서도 열려야 합니다.
- `main`에 push하면 GitHub Pages로 바로 배포되어 **시연 링크가 즉시 바뀝니다.** 시연 직전에는 push하지 마세요.
- 확인: `python -m http.server 8765` 후 `http://localhost:8765/start.html`
